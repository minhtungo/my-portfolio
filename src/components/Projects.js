import React, { useEffect, useRef } from 'react';

import { projects } from '../content/content';

const Projects = () => {
  return (
    <section id='projects'>
      
    </section>
  );
};

export default Projects;
